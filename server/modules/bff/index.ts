import type { BackendModule } from "../_shared/module";

export const bffModule: BackendModule = {
  name: "bff",
  responsibility:
    "Role-specific page DTOs, BffSection envelopes and frontend-facing projection contracts",
};
