NODE_ENV=development
PORT=5000

# mock | test | real
DATA_SOURCE_MODE=mock

# mock | local | test | neon
DATABASE_PROFILE=mock
DATABASE_URL=

REDIS_URL=
ALLOWED_ORIGINS=http://localhost:5000,http://127.0.0.1:5000

# mock | real
SCHEDULE_ADAPTER_MODE=mock
BOOKING_ADAPTER_MODE=mock
RAGIC_ADAPTER_MODE=mock
STORAGE_ADAPTER_MODE=mock
REPLIT_DATA_ADAPTER_MODE=mock

# External API sources.
INTERNAL_API_TOKEN=
LINE_BOT_BASE_URL=https://line-bot-assistant-ronchen2.replit.app
LINE_BOT_INTERNAL_TOKEN=
SMART_SCHEDULE_BASE_URL=https://smart-schedule-manager.replit.app
SMART_SCHEDULE_API_TOKEN=
EXTERNAL_API_TIMEOUT_MS=10000
RAGIC_API_KEY=
RAGIC_HOST=ap7.ragic.com
RAGIC_ACCOUNT_PATH=xinsheng
RAGIC_EMPLOYEE_SHEET=/ragicforms4/13

# Mock auth development account:
# username=1111
# password=1111
# default active role=system, switchable to employee/supervisor/system

# Optional migration bridge, only needed if another Replit service exposes BFF projections.
REPLIT_DATA_BASE_URL=
REPLIT_DATA_API_TOKEN=
REPLIT_DATA_TIMEOUT_MS=8000

SESSION_COOKIE_NAME=workbench_sid
SESSION_TTL_SECONDS=28800

# Legacy integrations, only needed when enabled.
GMAIL_USER=
GMAIL_APP_PASSWORD=
